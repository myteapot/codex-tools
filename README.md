# codex-useful-tools

This repository is now a Codex skill, not a bundle of one-click repair scripts.

Use `SKILL.md` as the operating guide for repairing Codex Desktop session
metadata, sidebar visibility, project grouping, and provider ID drift. The goal
is to help an agent inspect the local data model, make small reversible fixes,
and adapt to the Desktop version in front of it.

